

## ChatGVTL Modular Integration

The frontend now uses the modular core/chat/ui JavaScript stack. File names were normalized and HTML script loading was updated to initialize modules in dependency order.
